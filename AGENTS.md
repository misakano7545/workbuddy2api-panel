# 版本发布

不跟上游 `1.x`。发版只手动跑 `.github/workflows/binaries.yml`。

版本号是上海时间，形如 `v2026.9.26.711`：

- `2026.9.26` 是日期，月和日不补零
- `711` 是 7:11，时不补零、分两位、不带秒
- 二进制里只写 `2026.9.26.711`；面板会自己加 `v`
- 不要打 `v1.x` 标签，也不要手改 `appVersion`

本地构建不注入，面板显示 `dev`。同一分钟再发才会撞号。
